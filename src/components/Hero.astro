---
import { Icon } from 'astro-icon/components'
import { Image } from 'astro:assets'

/**
 * Hero Component
 *
 * @description A component that displays a hero section with a title, description, and image
 */
interface Props {
  /**
   * The source URL for the image
   */
  src?: string
}

const { src = '/undraw_bug-fixing_sgk7.svg' }: Props = Astro.props
---

<section class="hero my-24">
  <div class="container">
    <div class="grid grid-cols-1 items-center gap-24 lg:grid-cols-2">
      <div class="flex flex-col items-center gap-8 md:items-start">
        <h1 class="text-center text-6xl md:text-left lg:text-8xl">
          <slot><span class="text-gradient">Repair Café</span> Xanten</slot>
        </h1>
        

        
        <p class="text-center text-2xl md:text-left max-w-3xl">
          Seit Juli 2022 reparieren wir gemeinsam, was sich reparieren lässt. 
          Am letzten Samstag des Monats stehen ehrenamtliche Experten & Expertinnen zur Verfügung. 
          Bei Kaffee und Gebäck teilen wir Wissen und stärken das soziale Miteinander.
        </p>

        <div class="flex flex-col gap-3 min-[500px]:flex-row">
          <a class="button has-icon" href="#contact">
            <Icon name="lucide:phone" />
            Kontakt
          </a>
          <a class="button has-icon color-secondary" href="#services">
            <Icon name="lucide:wrench" />
            Unsere Services
          </a>
        </div>
      </div>
      <Image class="hidden lg:block" src={src} alt="" decoding="async" width={800} height={600} loading="eager" />
    </div>
    
    <!-- Next Event Date Display - Centered below main content -->
    <div class="event-card-container">
      <div class="next-event-card button-style">
        <Icon name="lucide:calendar" class="event-icon" />
        <div class="event-content">
          <div class="event-label">Das nächste Repair Café</div>
          <div class="event-date">28.08.2025</div>
          <div class="event-location">Haus der Begegnung, Karthaus 12</div>
        </div>
      </div>
    </div>
  </div>
  
</section>

<style lang="scss">
  h1 {
    text-wrap: unset;
  }

  .text-gradient {
    background: linear-gradient(
      315deg,
      light-dark(var(--color-primary-300), var(--color-secondary-100)) 25%,
      light-dark(var(--color-secondary-300), var(--color-primary-200))
    );
    background-clip: border-box;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .event-card-container {
    display: flex;
    justify-content: center;
    margin-top: 3rem;
    width: 100%;
  }

  .next-event-card {
    display: flex;
    align-items: center;
    gap: var(--space-s);
    padding: var(--space-s) var(--space-m);
    background-color: var(--color-neutral-100);
    border: 3px solid var(--color-neutral-700);
    border-radius: var(--radius-l);
    color: var(--color-neutral-900);
    text-decoration: none;
    font-weight: bold;
    transition: all var(--animation-speed-fast) var(--cubic-bezier);
    width: fit-content;

    &:hover {
      background-color: var(--color-neutral-700);
      color: var(--color-neutral-100);
    }

    .event-icon {
      width: 2rem;
      height: 2rem;
      flex-shrink: 0;
    }

    .event-content {
      display: flex;
      flex-direction: column;
      text-align: left;
    }

    .event-label {
      font-size: 0.95rem;
      font-weight: 500;
      opacity: 0.8;
      margin-bottom: 0.125rem;
    }

    .event-date {
      font-size: 1.25rem;
      font-weight: 900;
      line-height: 1;
      margin-bottom: 0.125rem;
    }

    .event-location {
      font-size: 0.75rem;
      font-weight: 500;
      opacity: 0.8;
    }
  }

  :global(.darkmode) .next-event-card {
    background-color: var(--color-neutral-900);
    border-color: var(--color-neutral-100);
    color: var(--color-neutral-100);

    &:hover {
      background-color: var(--color-neutral-100);
      color: var(--color-neutral-900);
    }
  }

  @media (max-width: 768px) {
    .event-card-container {
      margin-top: 2rem;
      padding: 0 1rem;
    }
    
    .next-event-card {
      width: 100%;
      max-width: 320px;
      
      .event-date {
        font-size: 1.125rem;
      }
      
      .event-icon {
        width: 1.5rem;
        height: 1.5rem;
      }
      
      .event-label {
        font-size: 0.875rem;
      }
      
      .event-location {
        font-size: 0.7rem;
      }
    }
  }
</style>